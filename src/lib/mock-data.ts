import type { ClassInfo, Booking } from "@/types";

// This file is no longer the primary source of truth for the calendar or bookings list.
// The app now generates classes dynamically and stores state in sessionStorage.
// This file is kept for reference or potential future use with other mock data.

export const MOCK_CLASSES: ClassInfo[] = [];
export const MOCK_USER_BOOKINGS: Booking[] = [];
